
library SafeMath {                                    }
contract ownable {
    
    function ownable() public {                     }
    modifier onlyOwner {                                              _; }               }
interface tokenRecipient {                                                   }
