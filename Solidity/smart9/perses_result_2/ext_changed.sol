contract BRDLockup            {
  uint256        unlockDate;
  function processInterval()                                                       {
    bool _correctInterval = now >= unlockDate                                                               ;
  }
  function pushAllocation(                                        )                  {
            now < unlockDate ;
  }
}
